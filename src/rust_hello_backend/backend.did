type Merchant = record { id : nat; products : vec nat };
service : {
  add_merchant : (nat) -> ();
  add_product : () -> (text);
  add_to_cart : () -> (text);
  delete_from_cart : () -> (text);
  get_cscart_products : (text, text, text) -> (text);
  get_merchant : (nat) -> (Merchant) query;
  remove_merchant : () -> (text);
  remove_product : () -> (text);
}