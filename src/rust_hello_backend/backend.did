service : {
  add_to_cart : () -> (text);
  delete_from_cart : () -> (text);
  delist_product : () -> (text);
  deregister_merchant : () -> (text);
  get_cart : () -> (text) query;
  get_merchant : (nat) -> () query;
  get_product : () -> (text);
  import_products : (text, text, text) -> (text);
  list_product : () -> (text);
  list_products : () -> (text) query;
  register_merchant : (nat) -> ();
  update_merchant : (nat) -> () query;
}